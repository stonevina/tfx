#! /usr/bin/env node

var path = require('path');

var entry = path.normalize(__dirname + '/../tfx.js');

var filename = process.argv.slice(2).toString();

var out = require(entry);

out.read(filename);
